
namespace AdventOfCode23.DayThree;

public class ProcessSchematic(char[,] Schematic)
{
    private readonly char[,] _schematic = Schematic;
    private readonly int _depth = Schematic.GetLength(0);
    private readonly int _width = Schematic.GetLength(1);

    
}
